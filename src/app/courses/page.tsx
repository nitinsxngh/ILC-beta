"use client";
import React from "react";
import NamesList from "@/components/NamesList"; // Adjust if needed
import MentorList from "@/components/MentorList"; // Adjust if needed

const CoursesPage = () => {
    return (
        <div>
            <NamesList />
            <MentorList />
        </div>
    );
};

export default CoursesPage;
